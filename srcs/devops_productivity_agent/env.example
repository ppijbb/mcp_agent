# DevOps Productivity Agent 환경변수 설정
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요

# =============================================================================
# 필수 환경변수 (Required Environment Variables)
# =============================================================================

# Google Gemini API 키 (필수)
# https://aistudio.google.com/app/apikey 에서 발급
GOOGLE_API_KEY=your-gemini-api-key-here

# GitHub API 토큰 (필수)
# https://github.com/settings/tokens 에서 발급
GITHUB_TOKEN=your-github-token-here

# AWS 자격증명 (필수)
# AWS IAM에서 Access Key 생성
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_DEFAULT_REGION=us-east-1

# =============================================================================
# 선택적 환경변수 (Optional Environment Variables)
# =============================================================================

# Prometheus 서버 URL (기본값: http://localhost:9090)
PROMETHEUS_URL=http://localhost:9090

# Google Cloud Platform 자격증명 (GCP MCP 서버용)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcp-service-account.json
GCP_PROJECT_ID=your-gcp-project-id

# Azure 자격증명 (Azure MCP 서버용)
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=your-azure-tenant-id
AZURE_SUBSCRIPTION_ID=your-azure-subscription-id

# Kubernetes 설정 (Kubernetes MCP 서버용)
KUBECONFIG=/path/to/kubeconfig
KUBERNETES_NAMESPACE=default

# =============================================================================
# 개발/디버깅 설정 (Development/Debug Settings)
# =============================================================================

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# MCP 서버 디버그 모드
MCP_DEBUG=false

# 출력 디렉토리 (기본값: devops_reports)
OUTPUT_DIR=devops_reports

# =============================================================================
# 설정 가이드 (Configuration Guide)
# =============================================================================

# 1. Google Gemini API 키 발급:
#    - https://aistudio.google.com/app/apikey 방문
#    - "Create API Key" 클릭
#    - 생성된 키를 GOOGLE_API_KEY에 설정

# 2. GitHub 토큰 발급:
#    - https://github.com/settings/tokens 방문
#    - "Generate new token (classic)" 클릭
#    - 필요한 권한 선택: repo, read:org, read:user
#    - 생성된 토큰을 GITHUB_TOKEN에 설정

# 3. AWS 자격증명 설정:
#    - AWS IAM 콘솔에서 사용자 생성
#    - 필요한 정책 연결: EC2FullAccess, S3FullAccess, LambdaFullAccess 등
#    - Access Key 생성 후 위 변수들에 설정

# 4. MCP 서버 설치:
#    npm install -g @modelcontextprotocol/server-aws-kb
#    npm install -g @modelcontextprotocol/server-github
#    npm install -g @modelcontextprotocol/server-prometheus
#    npm install -g @modelcontextprotocol/server-kubernetes
#    npm install -g @modelcontextprotocol/server-gcp
#    npm install -g @modelcontextprotocol/server-azure

# 5. 이 파일을 .env로 복사:
#    cp env.example .env
#    # .env 파일을 편집하여 실제 값으로 수정
