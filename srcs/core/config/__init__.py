"""
Configuration module.

Provides configuration loading, validation, and environment variable
substitution for sensitive values.

Classes:
    AppConfig: Main application configuration schema
    MCPServerConfig: MCP server configuration schema
    LoggingConfig: Logging configuration schema
    SecurityConfig: Security configuration schema
    CacheConfig: Cache configuration schema

Functions:
    load_config: Load and validate configuration files
    settings: Global configuration object
"""
