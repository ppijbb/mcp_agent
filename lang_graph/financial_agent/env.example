# Financial Agent 환경 변수 설정
# 모든 환경 변수는 필수입니다. 누락 시 시스템이 시작되지 않습니다.

# =============================================================================
# LLM 설정 (Gemini 2.5 Flash Lite 우선 사용)
# =============================================================================
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash-lite
LLM_TEMPERATURE=0.7

# =============================================================================
# MCP 서버 설정
# =============================================================================
MCP_TIMEOUT=30
MCP_DATA_PERIOD=3mo

# =============================================================================
# 워크플로우 설정
# =============================================================================
# 유효한 리스크 프로필 (쉼표로 구분)
RISK_PROFILES=conservative,moderate,aggressive

# 기본 분석 대상 티커 (쉼표로 구분)
DEFAULT_TICKERS=NVDA,AMD,QCOM,AAPL,MSFT

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================
# 1. 모든 환경 변수는 필수입니다. 누락 시 시스템이 시작되지 않습니다.
# 2. GEMINI_API_KEY는 Google AI Studio에서 발급받아야 합니다.
# 3. LLM_TEMPERATURE는 0.0-2.0 범위여야 합니다.
# 4. MCP_TIMEOUT은 양수여야 합니다.
# 5. RISK_PROFILES는 정확히 "conservative,moderate,aggressive"여야 합니다.
# 6. DEFAULT_TICKERS는 유효한 주식 티커여야 합니다.
# 7. 이 파일을 .env로 복사하고 실제 값으로 수정하세요.
