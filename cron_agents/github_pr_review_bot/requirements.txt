# GitHub PR Review Bot - Gemini CLI 기반 무료 버전
# NO FALLBACK MODE - 모든 오류는 즉시 상위로 전파

# GitHub API
PyGithub>=2.7.0

# 웹 프레임워크 (최소 구성)
fastapi>=0.116.1
uvicorn>=0.35.0
python-dotenv>=1.1.1
pydantic>=2.11.7
requests>=2.32.4

# AI 라이브러리
openai>=1.98.0

# 캐시 및 성능 최적화
redis>=6.2.0
tenacity>=9.1.2

# 로깅 (간소화)
structlog>=25.4.0

# 개발 도구 (필수만)
pytest>=8.4.1
black>=25.1.0

# 시스템 모니터링 (최소)
psutil>=7.0.0

# MCP (Model Context Protocol) 서버들
# 실제 MCP 서버들은 npm으로 설치됨:
# npm install @modelcontextprotocol/server-filesystem
# npm install @modelcontextprotocol/server-memory  
# npm install @modelcontextprotocol/server-sequential-thinking

# 무료 AI 리뷰를 위한 로컬 분석 도구
# ast는 Python 내장 모듈이므로 별도 설치 불필요
# gemini-cli는 npm으로 별도 설치 필요: npm install -g @google/gemini-cli