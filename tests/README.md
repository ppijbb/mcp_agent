# Tests

- **Pytest suite**: `core/`, `test_*.py` — run from repo root: `pytest tests/` or `python -m pytest tests/`
- **Hello server example**: `hello_server_example/` — Smithery MCP scaffold; run from that dir: `cd tests/hello_server_example && uv run dev`
